# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-cli
dist-ssr
*.local

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Browser checks: throwaway scripts plus their own puppeteer install, kept out of
# the app's dependencies entirely. See docs/design.md, "Measuring".
tools/visual/
# Older root-level throwaway scripts, still ignored so a stray one can't land.
smoke.mjs
perf.mjs
# Throwaway analysis runs — real modules over a locally fetched payload.
src/**/*.explore.test.ts
